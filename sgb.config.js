module.exports = {
  styleGuidDir: '', // directory sgb will look in for markup/base, markup/patterns, docs/base, and docs/patterns
  buildDir: 'build', // generated directory that built files are written to
};